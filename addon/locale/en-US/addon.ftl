menu-generate-tags = Generate AI Tags
prefs-title = AI Tags
progress-start = Generating tags from abstract and first-page PDF text
progress-queue-empty = Loaded { $total } items and ready to start. Queued { $queued }, requesting { $requesting }, completed { $completed }. Concurrency { $concurrency }, RPS { $rps }
progress-item-queued = Item { $current } / { $total } queued: { $title }. Queued { $queued }, requesting { $requesting }, completed { $completed }. Concurrency { $concurrency }, RPS { $rps }
progress-item-requesting = Item { $current } / { $total } requesting: { $title }. Queued { $queued }, requesting { $requesting }, completed { $completed }. Concurrency { $concurrency }, RPS { $rps }
progress-item-completed = Item { $current } / { $total } completed: { $title }. Queued { $queued }, requesting { $requesting }, completed { $completed }. Concurrency { $concurrency }, RPS { $rps }
progress-summary = AI tag generation finished: { $success } succeeded, { $skipped } skipped, { $failed } failed
progress-failure = AI tag generation failed: { $message }
error-no-selection = Please select at least one regular Zotero item
error-disabled = The plugin is disabled in settings
error-missing-api-base = Please configure API Base URL in settings
error-missing-api-key = Please configure API Key in settings
error-missing-model = Please configure model name in settings
error-invalid-api-extra-params = API extra params must be a valid JSON object
api-test-start = Testing API connectivity
api-test-success = API connectivity test succeeded
api-test-failure = API connectivity test failed: { $message }
llm-config-switched = Switched to { $name }
llm-config-added = Created { $name }
llm-config-removed = Deleted { $name }
llm-config-minimum = Keep at least one AI config
llm-config-remove-confirm = Delete the current AI config "{ $name }"?
collection-rule-select-required = Please select a collection first
collection-rule-added = Collection rule added: { $path }
collection-rule-removed = Collection rule removed: { $path }
collection-rule-empty = No collection-specific rules configured yet.
collection-rule-unknown = Unknown collection
collection-rule-remove = Remove
collection-rule-applies = This rule applies to the current collection and all of its subcollections, and is appended after the global tagging rules.
collection-rule-missing = This collection no longer exists or is unavailable. Please remove or recreate this rule.
collection-rule-placeholder = Enter collection-specific tagging requirements...
collection-rule-select-placeholder = Select a collection
collection-rule-conflict-same = This collection already has a rule: { $path }
collection-rule-conflict-descendant = This collection is a subcollection of an existing rule, so it cannot be added again: { $path }
collection-rule-conflict-ancestor = An existing rule already targets a subcollection of this collection, so this would overlap: { $path }
